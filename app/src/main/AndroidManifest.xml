<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CALL_PHONE"/>

    <application
        android:excludeFromRecents="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:usesCleartextTraffic="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="m">
        <activity android:name=".ELpdf" />
        <activity android:name=".oldBook"
            android:parentActivityName=".getmain"/>
        <activity android:name=".pdf"
            android:parentActivityName=".getmain"/>
        <activity android:name=".register"
            android:parentActivityName=".login"/>
        <activity android:name=".ELoldbook" />
        <activity android:name=".login"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".PostBook"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".getmain" />
        <activity android:name=".historypost" />
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>